# ================================================================
# SGOHA CSP Service - Variables de entorno
# Copia a .env y completa los valores reales. Nunca commitees el .env.
# ================================================================

PORT=8000
LOG_LEVEL=INFO

# RNF-01: el motor CSP debe completarse en <= 30s para 50 cursos.
SOLVER_TIMEOUT_SECONDS=30
